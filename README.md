# 胶片分镜条核对台

一个纯前端本地应用，用来整理纸质放映胶片片段、拖拽调整放映顺序、统计总时长、筛选颜色偏移并生成试映前破损提醒。

试映时可让任意一段进入"放映中"，现场点击掌声 / 冷场 / 提问即时在卡片上累计（三类反应分色显示）；同一时刻只有一段在放映，换段前需先结束本轮。记录随片段走：拖动换序不丢、删除片段即清空，重开页面后数据仍在。总览栏实时显示正在放映、已记录片段数与反应总数。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。
